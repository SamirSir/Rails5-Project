<h1>Welcome index</h1>
<p>Find me in app/views/welcome/index.html.erb</p>
<h1>Haha Gais</h1>
<%# this how we put comment %>

<%  7 .times do |i| %>
	<% if i%2 == 0 %>
		<%= i.to_s+". This is  so romantic" %>
	<% else %>
		<%= "\nthis is odd count " %>
	<% end %>
	<br>
<% end %> 
<br>
<%= link_to("Go to Sample", 
	{
	:controller => 'welcome',
	:action => 'sample',
	:number => 421,
	:message => "I love Learning",
	}) %>
<%# :controller => 'welcome' is optional for same view container %>
<br>

<% if(params.has_key?(:id) &&
  		params.has_key?(:random)) %><br>
	<%= "ID : #{params[:id]}" %><br>
	<%= "RANDOM : #{params[:random]}" %><br>
<% end %>
